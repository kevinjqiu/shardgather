============
Installation
============

At the command line::

    $ easy_install shardgather

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv shardgather
    $ pip install shardgather